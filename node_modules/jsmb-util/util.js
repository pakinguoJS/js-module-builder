var fs = require('fs');
var path = require('path');

module.exports = (function(){

    function mkdir(dst){
        if(!fs.existsSync(dst)){
            mkdir(path.dirname(dst));
            fs.mkdirSync(dst);
        }
    }

    return {
        mkdir: mkdir
    };
})();