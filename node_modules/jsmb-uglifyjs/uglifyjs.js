var grunt = require('grunt');
var colors = require('colors');
var urlifyjs = require('uglify-js');


module.exports = function (dest, files, options) {
    grunt.file.write(dest, urlifyjs.minify(files).code);

    // console.log('[Succ]: created file '.green + fileObj.dest);
}